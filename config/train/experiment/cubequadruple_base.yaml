# @package _global_
# cubequadruple: LeWM baseline (pixels, SIGReg 0.09). Data: self-collected OGB multi-object.
defaults:
  - override /data: ogb_quadruple

output_model_name: lewm_cubequadruple_base_s${seed}
subdir: ${output_model_name}

trainer:
  max_epochs: 10
  devices: 1

loss:
  obj:
    weight: 0.0
    q_variant: cube_double_full
