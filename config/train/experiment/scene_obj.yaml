# @package _global_
# scene: SCALE (SIGReg + L_obj 0.1 on FULL-CONFIG q, weight carried from the cube family).
defaults:
  - override /data: ogb_scene

output_model_name: lewm_scene_obj0.1_s${seed}
subdir: ${output_model_name}

trainer:
  max_epochs: 10
  devices: 1

loss:
  obj:
    weight: 0.1
    q_variant: scene_full
