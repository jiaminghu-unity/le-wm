# @package _global_
# GATED full-q SCALE: L_obj on sqrt(g*) x q with the lambda=0.5 Stage-1 gate
# (train_qgate2.py, QGATE_GCS injected by the chain). Derived from pusht_scale_native.yaml.
output_model_name: lewm_pusht_scale_gate05_s${seed}
subdir: ${output_model_name}

trainer:
  max_epochs: 10
  devices: 1

loss:
  obj:
    weight: 0.1
    q_variant: pusht_state_native
  aux:
    weight: 0.0
