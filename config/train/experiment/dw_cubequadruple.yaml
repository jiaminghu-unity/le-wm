# @package _global_
# DINO-WM on cubequadruple (frozen dinov2 encoder). Mirrors dw_cube.yaml: same data/loader/
# frameskip/history/epochs as the LeWM arms, model family is the only variable.
# Same undertraining caveat (paper ~100 epochs vs our 10, bias AGAINST this arm).
defaults:
  - override /model: prejepa
  - override /data: ogb_quadruple

output_model_name: dinowm_cubequadruple_s${seed}
subdir: ${output_model_name}

trainer:
  max_epochs: 10
  devices: 1

loader:
  batch_size: 32
